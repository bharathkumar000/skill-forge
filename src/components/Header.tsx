import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="py-8 bg-transparent relative z-[100]">
      {/* Intentionally Minimal / Cleaned up */}
    </header>
  );
};

export default Header;
